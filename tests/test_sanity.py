"""Minimal test suite.

This test ensures that the test harness itself is working. It can be
extended with more meaningful tests when running in an environment
that has access to external dependencies.
"""


def test_sanity():
    """A basic sanity check that always passes."""
    assert True